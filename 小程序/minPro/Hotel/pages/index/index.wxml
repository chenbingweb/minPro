<view class="fixed flex_center index_page">
  <view class="btn ok_active" bindtap="onScanle">
    核销
  </view>
  <view class="btn" bindtap="onToList">
    记录
  </view>
</view>
<!-- 注册  wx:if="{{showSign}}"-->
<regiser isClose="{{showLogin}}" bindisLogin="isLogin" wx:if="{{showLogin}}"/>
<!-- <view class="sign_box" wx:if="{{showLogin}}">
    <view class="fill_content flex_center">
      <view class="input_item flex_start">
        <view>手机号</view>
        <input class="input" placeholder="请输入手机号"></input>
        <view class="do_btn">获取验证码</view>
      </view>
       <view class="input_item flex_start">
        <view>验证码</view>
        <input  class="input"  placeholder="请输入手机号"></input>
        <view class="do_btn">注册</view>
      </view>
      <view class="close_btn icon_box" bindtap="onClose">
        <image src="../../images/del.png" style="width:50rpx;height:50rpx"></image>
      </view>
    </view> 
</view> -->