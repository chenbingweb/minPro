<view class="fixed">
  <paging  dataObj="{{dataObj}}" bindpageData="pageData">
    <view slot="list" class="list">
        <view class="item_list flex_between f_30_333" wx:for="{{list}}" wx:key="list_{{index}}">
          <view>
            {{item.name}}
          </view>
          <view>
            {{item.price}}<text style="font-size:24rpx;">¥</text>
          </view>
        </view>
    </view>
  </paging>
</view>